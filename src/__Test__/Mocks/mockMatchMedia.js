Object.defineProperty(window, 'matchMedia', {
    writable: true,
    value: jest.fn().mockImplementation(query => ({
      matches: false,
      media: query,
      onchange: null,
      addListener: jest.fn(), // For compatibility purposes
      removeListener: jest.fn(), // For compatibility purposes
    })),
  });
  